# PayPal .NET integration plan — eShopOnWeb PublicApi

## 1. Scope & sequence

1. Add the pinned `AsadAli.Checkout.Sdk` `1.0.1` package, register one long-lived SDK client, configure OAuth credentials and `ServerEnvironment.Sandbox`, and load the resilience settings before exposing PublicApi endpoints.
2. Create checkout orders with `Orders.CreateOrder`; return the order id and the `rel=approve` HATEOAS link from `Order.Links`, then authorize with `Orders.AuthorizeOrder` when the order intent is `AUTHORIZE` or capture with `Orders.CaptureOrder` when the intent is `CAPTURE`.
3. Support authorized-payment cancellation with `Payments.VoidPayment`; support full or partial refunds with `Payments.RefundCapturedPayment`; use the corresponding read operations for status reconciliation where needed.
4. Create temporary setup tokens and customer payment tokens with `Vault.CreateSetupToken`/`Vault.CreatePaymentToken`; read, list, and delete saved payment tokens with the remaining `Vault` operations.
5. Reconcile by calling `TransactionSearch.SearchTransactions` (and `SearchBalances` when balance snapshots are required), explicitly paging from the returned page metadata because the SDK supplies no automatic pagination.
6. Apply the error boundary and idempotency policy around every write. Pass the exact PayPal request-id parameter on supported write operations; application-level replay/concurrency/persistence policy is outside this SDK contract.

## 2. CONTRACT SHEET

> **Signatures are generated code, verbatim — every parameter name is the literal
> C# identifier. The cancellation-token parameter really is named `ct`: in named
> arguments write `ct:`, never `cancellationToken:`.**
>
> **Every SDK type is written fully-qualified with the namespace the map gives it** — take
> each one from that type's own map row, never from where a neighbouring type sits. A members
> table names the namespace outright; otherwise the row's source path implies it
> (`Core/Configuration/…` ⇒ `…Core.Configuration`; a file at the repo root ⇒ the root
> namespace). Enums, unions, auth, server and client-config types are spread across different
> child namespaces, and two types configured side by side in the same options object routinely
> live in different ones. Dropping a type to the root or to `.Models` makes the implementer
> guess the wrong `using`, and the build breaks.

All operations are async Task-returning, throw-only; no `…Result` variants exist. `RequestOptions` is `PayPalServerSdk.Core.RequestOptions`; `SdkException<T>` is `PayPalServerSdk.Core.Exceptions.SdkException<T>`; `RawError` is `PayPalServerSdk.Core.ErrorResponse.RawError`; generated records are `PayPalServerSdk.Models.*`; enums are `PayPalServerSdk.Models.Enums.*`; typed errors are `PayPalServerSdk.Errors.*`.

| Caller / operation | Exact generated signature | Request model and fields used | Response envelope / fields read | Error contract | Pagination | Source |
|---|---|---|---|---|---|---|
| `client.Orders.CreateOrder` | `CreateOrder(string? payPalMockResponse, string? payPalRequestId, string? payPalPartnerAttributionId, string? payPalClientMetadataId, string? payPalAuthAssertion, PayPalServerSdk.Models.OrderRequest body, string? prefer = "return=minimal", PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | `OrderRequest`: `Intent (intent): PayPalServerSdk.Models.Enums.CheckoutPaymentIntent` required; `Payer (payer): PayPalServerSdk.Models.Payer?`; `PurchaseUnits (purchase_units): IReadOnlyList<PayPalServerSdk.Models.PurchaseUnitRequest>` required; `PaymentSource (payment_source): PayPalServerSdk.Models.PaymentSource?`; `ApplicationContext (application_context): PayPalServerSdk.Models.OrderApplicationContext?`. `PurchaseUnitRequest`: `ReferenceId (reference_id): string?`, `Amount (amount): PayPalServerSdk.Models.AmountWithBreakdown` required, `Payee (payee): PayPalServerSdk.Models.PayeeBase?`, `PaymentInstruction (payment_instruction): PayPalServerSdk.Models.PaymentInstruction?`, `Description (description): string?`, `CustomId (custom_id): string?`, `InvoiceId (invoice_id): string?`, `SoftDescriptor (soft_descriptor): string?`, `Items (items): IReadOnlyList<PayPalServerSdk.Models.ItemRequest>?`, `Shipping (shipping): PayPalServerSdk.Models.ShippingDetails?`, `SupplementaryData (supplementary_data): PayPalServerSdk.Models.SupplementaryData?`. `AmountWithBreakdown`: `CurrencyCode (currency_code): string` required, `Value (value): string` required, `Breakdown (breakdown): PayPalServerSdk.Models.AmountBreakdown?`. `Money`: `CurrencyCode (currency_code): string` required, `Value (value): string` required. `OrderApplicationContext` fields: `BrandName (brand_name): string?`, `Locale (locale): string?`, `LandingPage (landing_page): PayPalServerSdk.Models.Enums.OrderApplicationContextLandingPage?`, `ShippingPreference (shipping_preference): PayPalServerSdk.Models.Enums.OrderApplicationContextShippingPreference?`, `UserAction (user_action): PayPalServerSdk.Models.Enums.OrderApplicationContextUserAction?`, `PaymentMethod (payment_method): PayPalServerSdk.Models.PaymentMethodPreference?`, `ReturnUrl (return_url): string?`, `CancelUrl (cancel_url): string?`, `StoredPaymentSource (stored_payment_source): PayPalServerSdk.Models.StoredPaymentSource?`. Notes require a valid order body; no additional Notes-named fields are required by the map. | `PayPalServerSdk.Models.Order`: `Id (id): string?`, `Status (status): PayPalServerSdk.Models.Enums.OrderStatus?`, `Intent (intent): PayPalServerSdk.Models.Enums.CheckoutPaymentIntent?`, `PurchaseUnits (purchase_units): IReadOnlyList<PayPalServerSdk.Models.PurchaseUnit>?`, `PaymentSource (payment_source): PayPalServerSdk.Models.PaymentSourceResponse?`, `Payer (payer): PayPalServerSdk.Models.Payer?`, `Links (links): IReadOnlyList<PayPalServerSdk.Models.LinkDescription>?`, `CreateTime (create_time): string?`, `UpdateTime (update_time): string?`. Read `Links` for `Href (href): string` required, `Rel (rel): string` required, `Method (method): PayPalServerSdk.Models.Enums.LinkHttpMethod?`. | `PayPalServerSdk.Core.Exceptions.SdkException<PayPalServerSdk.Errors.CreateOrderError>` Case A; `TryGetError(out PayPalServerSdk.Models.Error)` for 400/401/422, then `TryGetRawError(out PayPalServerSdk.Core.ErrorResponse.RawError)`. | none | `operations/Orders.md`; `records-1-Ac-Pa.md`; `records-2-Pa-Ve.md` |
| `client.Orders.AuthorizeOrder` | `AuthorizeOrder(string id, string? payPalMockResponse, string? payPalRequestId, string? payPalClientMetadataId, string? payPalAuthAssertion, PayPalServerSdk.Models.OrderAuthorizeRequest? body, string? prefer = "return=minimal", PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | Nullable parameters `payPalMockResponse` through `body` have no defaults and must be passed explicitly. `OrderAuthorizeRequest`: `PaymentSource (payment_source): PayPalServerSdk.Models.OrderAuthorizeRequestPaymentSource?`; nested payment-source fields: `Card (card): PayPalServerSdk.Models.CardRequest?`, `Token (token): PayPalServerSdk.Models.Token?`, `Paypal (paypal): PayPalServerSdk.Models.PayPalWallet?`, `ApplePay (apple_pay): PayPalServerSdk.Models.ApplePayRequest?`, `GooglePay (google_pay): PayPalServerSdk.Models.GooglePayRequest?`, `Venmo (venmo): PayPalServerSdk.Models.VenmoWalletRequest?`. The Notes require buyer approval or a valid `payment_source`. | `PayPalServerSdk.Models.OrderAuthorizeResponse`: `Id (id): string?`, `Status (status): PayPalServerSdk.Models.Enums.OrderStatus?`, `PurchaseUnits (purchase_units): IReadOnlyList<PayPalServerSdk.Models.PurchaseUnit>?`, `PaymentSource (payment_source): PayPalServerSdk.Models.OrderAuthorizeResponsePaymentSource?`, `Payer (payer): PayPalServerSdk.Models.Payer?`, `Links (links): IReadOnlyList<PayPalServerSdk.Models.LinkDescription>?`, `CreateTime (create_time): string?`, `UpdateTime (update_time): string?`. | `SdkException<PayPalServerSdk.Errors.AuthorizeOrderError>` Case A; `TryGetError(out PayPalServerSdk.Models.Error)` [400,401,403,404,422,500], then `TryGetRawError`. | none | `operations/Orders.md`; `records-1-Ac-Pa.md`; `records-2-Pa-Ve.md` |
| `client.Orders.CaptureOrder` | `CaptureOrder(string id, string? payPalMockResponse, string? payPalRequestId, string? payPalClientMetadataId, string? payPalAuthAssertion, PayPalServerSdk.Models.OrderCaptureRequest? body, string? prefer = "return=minimal", PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | Nullable parameters `payPalMockResponse` through `body` have no defaults and must be explicit. `OrderCaptureRequest`: `PaymentSource (payment_source): PayPalServerSdk.Models.OrderCaptureRequestPaymentSource?`; nested fields: `Card (card): PayPalServerSdk.Models.CardRequest?`, `Token (token): PayPalServerSdk.Models.Token?`, `Paypal (paypal): PayPalServerSdk.Models.PayPalWallet?`, `ApplePay (apple_pay): PayPalServerSdk.Models.ApplePayRequest?`, `GooglePay (google_pay): PayPalServerSdk.Models.GooglePayRequest?`, `Venmo (venmo): PayPalServerSdk.Models.VenmoWalletRequest?`. Notes require buyer approval or a valid `payment_source`. | `PayPalServerSdk.Models.Order`: fields as in `CreateOrder`; inspect `Status`, `PurchaseUnits`, and `Links`. | `SdkException<PayPalServerSdk.Errors.CaptureOrderError>` Case A; `TryGetError(out PayPalServerSdk.Models.Error)` [400,401,403,404,422,500], then `TryGetRawError`. | none | `operations/Orders.md`; `records-1-Ac-Pa.md`; `records-2-Pa-Ve.md` |
| `client.Payments.VoidPayment` (cancel) | `VoidPayment(string authorizationId, string? payPalMockResponse, string? payPalAuthAssertion, string? payPalRequestId, string? prefer = "return=minimal", PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | No body. Nullable header parameters have no defaults and must be explicit. Notes: cannot void an authorization that has been fully captured. | `PayPalServerSdk.Models.PaymentAuthorization`: `Id (id): string?`, `Status (status): PayPalServerSdk.Models.Enums.AuthorizationStatus?`, `StatusDetails (status_details): PayPalServerSdk.Models.AuthorizationStatusDetails?`, `Amount (amount): PayPalServerSdk.Models.Money?`, `ExpirationTime (expiration_time): string?`, `Links (links): IReadOnlyList<PayPalServerSdk.Models.LinkDescription>?`, `CreateTime (create_time): string?`, `UpdateTime (update_time): string?`. | `SdkException<PayPalServerSdk.Errors.VoidPaymentError>` Case A; `TryGetError(out PayPalServerSdk.Models.Error)` [401,403,404,409,422], `TryGetNoContent(out RawError)` [500], then `TryGetRawError(out RawError)`. | none | `operations/Payments.md`; `records-2-Pa-Ve.md` |
| `client.Payments.RefundCapturedPayment` | `RefundCapturedPayment(string captureId, string? payPalMockResponse, string? payPalRequestId, string? payPalAuthAssertion, PayPalServerSdk.Models.RefundRequest? body, string? prefer = "return=minimal", PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | Nullable parameters `payPalMockResponse` through `body` have no defaults and must be explicit. `RefundRequest`: `Amount (amount): PayPalServerSdk.Models.Money?`, `CustomId (custom_id): string?`, `InvoiceId (invoice_id): string?`, `NoteToPayer (note_to_payer): string?`, `PaymentInstruction (payment_instruction): PayPalServerSdk.Models.RefundPaymentInstruction?`. Notes: full refund uses an empty JSON body; partial refund includes `amount`. | `PayPalServerSdk.Models.Refund`: `Id (id): string?`, `Status (status): PayPalServerSdk.Models.Enums.RefundStatus?`, `Amount (amount): PayPalServerSdk.Models.Money?`, `InvoiceId (invoice_id): string?`, `CustomId (custom_id): string?`, `NoteToPayer (note_to_payer): string?`, `Links (links): IReadOnlyList<PayPalServerSdk.Models.LinkDescription>?`, `CreateTime (create_time): string?`, `UpdateTime (update_time): string?`. | `SdkException<PayPalServerSdk.Errors.RefundCapturedPaymentError>` Case A; `TryGetError(out PayPalServerSdk.Models.Error)` [400,401,403,404,409,422], `TryGetNoContent(out RawError)` [500], then `TryGetRawError(out RawError)`. | none | `operations/Payments.md`; `records-2-Pa-Ve.md` |
| `client.Payments.CaptureAuthorizedPayment` | `CaptureAuthorizedPayment(string authorizationId, string? payPalMockResponse, string? payPalRequestId, string? payPalAuthAssertion, PayPalServerSdk.Models.CaptureRequest? body, string? prefer = "return=minimal", PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | `CaptureRequest`: `Amount (amount): PayPalServerSdk.Models.Money?`, `InvoiceId (invoice_id): string?`, `FinalCapture (final_capture): bool?`, `PaymentInstruction (payment_instruction): PayPalServerSdk.Models.CapturePaymentInstruction?`, `NoteToPayer (note_to_payer): string?`, `SoftDescriptor (soft_descriptor): string?`. | `PayPalServerSdk.Models.CapturedPayment`: `Id (id): string?`, `Status (status): PayPalServerSdk.Models.Enums.CaptureStatus?`, `Amount (amount): PayPalServerSdk.Models.Money?`, `FinalCapture (final_capture): bool?`, `ProcessorResponse (processor_response): PayPalServerSdk.Models.ProcessorResponse?`, `Links (links): IReadOnlyList<PayPalServerSdk.Models.LinkDescription>?`, `CreateTime (create_time): string?`, `UpdateTime (update_time): string?`. | `SdkException<PayPalServerSdk.Errors.CaptureAuthorizedPaymentError>` Case A; `TryGetError(out PayPalServerSdk.Models.Error)` [400,401,403,404,409,422], `TryGetNoContent(out RawError)` [500], then `TryGetRawError(out RawError)`. | none | `operations/Payments.md`; `records-1-Ac-Pa.md` |
| `client.Vault.CreateSetupToken` | `CreateSetupToken(string? payPalRequestId, PayPalServerSdk.Models.SetupTokenRequest body, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | `SetupTokenRequest`: `Customer (customer): PayPalServerSdk.Models.Customer?`; `PaymentSource (payment_source): PayPalServerSdk.Models.SetupTokenRequestPaymentSource` required. Its source fields: `Card (card): PayPalServerSdk.Models.SetupTokenRequestCard?`, `Paypal (paypal): PayPalServerSdk.Models.VaultPayPalWalletRequest?`, `Venmo (venmo): PayPalServerSdk.Models.VaultVenmoRequest?`, `ApplePay (apple_pay): PayPalServerSdk.Models.VaultApplePayRequest?`, `Token (token): PayPalServerSdk.Models.VaultTokenRequest?`, `Bank (bank): PayPalServerSdk.Models.BankRequest?`. | `PayPalServerSdk.Models.SetupTokenResponse`: `Id (id): string?`, `Customer (customer): PayPalServerSdk.Models.Customer?`, `Status (status): PayPalServerSdk.Models.Enums.PaymentTokenStatus?`, `PaymentSource (payment_source): PayPalServerSdk.Models.SetupTokenResponsePaymentSource?`, `Links (links): IReadOnlyList<PayPalServerSdk.Models.LinkDescription>?`. | `SdkException<PayPalServerSdk.Errors.CreateSetupTokenError>` Case A; `TryGetError1(out PayPalServerSdk.Models.Error1)` [400,403,422,500], then `TryGetRawError(out RawError)`. | none | `operations/Vault.md`; `records-2-Pa-Ve.md` |
| `client.Vault.CreatePaymentToken` | `CreatePaymentToken(string? payPalRequestId, PayPalServerSdk.Models.PaymentTokenRequest body, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | `PaymentTokenRequest`: `Customer (customer): PayPalServerSdk.Models.Customer?`; `PaymentSource (payment_source): PayPalServerSdk.Models.PaymentTokenRequestPaymentSource` required. Source fields: `Card (card): PayPalServerSdk.Models.PaymentTokenRequestCard?`, `Token (token): PayPalServerSdk.Models.VaultTokenRequest?`. | `PayPalServerSdk.Models.PaymentTokenResponse`: `Id (id): string?`, `Customer (customer): PayPalServerSdk.Models.CustomerResponse?`, `PaymentSource (payment_source): PayPalServerSdk.Models.PaymentTokenResponsePaymentSource?`, `Links (links): IReadOnlyList<PayPalServerSdk.Models.LinkDescription>?`. | `SdkException<PayPalServerSdk.Errors.CreatePaymentTokenError>` Case A; `TryGetError1(out PayPalServerSdk.Models.Error1)` [400,403,404,422,500], then `TryGetRawError(out RawError)`. | none | `operations/Vault.md`; `records-2-Pa-Ve.md` |
| `client.Vault.GetSetupToken` / `GetPaymentToken` | `GetSetupToken(string id, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)`; `GetPaymentToken(string id, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | none | `SetupTokenResponse` or `PaymentTokenResponse` as above. | `SdkException<GetSetupTokenError>`: `TryGetError1(out Error1)` [403,404,422,500], raw fallback. `SdkException<GetPaymentTokenError>`: `TryGetError1(out Error1)` [403,404,422,500], raw fallback. | none | `operations/Vault.md`; `records-2-Pa-Ve.md` |
| `client.Vault.ListCustomerPaymentTokens` | `ListCustomerPaymentTokens(string customerId, int? pageSize = 5, int? page = 1, bool? totalRequired = false, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | Query wire names: `customer_id` ← `customerId`, `page_size` ← `pageSize`, `page` ← `page`, `total_required` ← `totalRequired`. | `PayPalServerSdk.Models.CustomerVaultPaymentTokensResponse`: `TotalItems (total_items): int?`, `TotalPages (total_pages): int?`, `Customer (customer): PayPalServerSdk.Models.VaultResponseCustomer?`, `PaymentTokens (payment_tokens): IReadOnlyList<PayPalServerSdk.Models.PaymentTokenResponse>?`, `Links (links): IReadOnlyList<PayPalServerSdk.Models.LinkDescription>?`. | `SdkException<ListCustomerPaymentTokensError>` Case A; `TryGetError1(out PayPalServerSdk.Models.Error1)` [400,403,500], then raw fallback. | No SDK pagination helper; only `page`, no `perPage`; use `TotalPages`/`TotalItems`. | `operations/Vault.md`; `records-1-Ac-Pa.md` |
| `client.Vault.DeletePaymentToken` | `DeletePaymentToken(string id, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | none | `void` Task. | `SdkException<DeletePaymentTokenError>` Case A; `TryGetError1(out PayPalServerSdk.Models.Error1)` [400,403,500], then raw fallback. | none | `operations/Vault.md`; `records-1-Ac-Pa.md` |
| `client.Payments.GetCapturedPayment` / `GetRefund` / `GetAuthorizedPayment` | `GetCapturedPayment(string captureId, string? payPalMockResponse, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)`; `GetRefund(string refundId, string? payPalMockResponse, string? payPalAuthAssertion, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)`; `GetAuthorizedPayment(string authorizationId, string? payPalMockResponse, string? payPalAuthAssertion, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | Nullable non-default parameters must be explicit. | `CapturedPayment`, `Refund`, and `PaymentAuthorization`; read `Id`, `Status`, `Amount`, and `Links` fields listed above. | `GetCapturedPaymentError`, `GetRefundError`, `GetAuthorizedPaymentError` are Case A; each has `TryGetError(out PayPalServerSdk.Models.Error)` for [401,403,404], `TryGetNoContent(out RawError)` [500] where listed by Payments, and `TryGetRawError` fallback. | none | `operations/Payments.md`; `records-1-Ac-Pa.md`; `records-2-Pa-Ve.md` |
| `client.TransactionSearch.SearchTransactions` | `SearchTransactions(string startDate, string endDate, string? transactionId, string? transactionType, string? transactionStatus, string? transactionAmount, string? transactionCurrency, string? paymentInstrumentType, string? storeId, string? terminalId, string? fields = "transaction_info", string? balanceAffectingRecordsOnly = "Y", int? pageSize = 100, int? page = 1, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | Required `startDate`, `endDate`; nullable `transactionId` through `terminalId` have no defaults and must be explicit. Wire query mapping: `start_date`, `end_date`, `transaction_id`, `transaction_type`, `transaction_status`, `transaction_amount`, `transaction_currency`, `payment_instrument_type`, `store_id`, `terminal_id`, `fields`, `balance_affecting_records_only`, `page_size`, `page`. | `PayPalServerSdk.Models.SearchResponse`: `TransactionDetails (transaction_details): IReadOnlyList<PayPalServerSdk.Models.TransactionDetails>?`, `StartDate (start_date): string?`, `EndDate (end_date): string?`, `Page (page): int?`, `TotalItems (total_items): int?`, `TotalPages (total_pages): int?`, `Links (links): IReadOnlyList<PayPalServerSdk.Models.LinkDescription>?`. Nested `TransactionDetails.TransactionInfo (transaction_info): PayPalServerSdk.Models.TransactionInformation?`; reconciliation fields include `TransactionId (transaction_id): string?`, `TransactionEventCode (transaction_event_code): string?`, `TransactionInitiationDate (transaction_initiation_date): string?`, `TransactionUpdatedDate (transaction_updated_date): string?`, `TransactionAmount (transaction_amount): PayPalServerSdk.Models.Money?`, `TransactionStatus (transaction_status): string?`, `InvoiceId (invoice_id): string?`, `PaymentMethodType (payment_method_type): string?`, `InstrumentType (instrument_type): string?`, `EndingBalance (ending_balance): PayPalServerSdk.Models.Money?`. | `SdkException<PayPalServerSdk.Core.ErrorResponse.RawError>` Case B; use `StatusCode`, `ReadAsString()`, `ReadAsJson<T>()`, or `ReadAsBytes()`. | No helper; `page` only, no `perPage`; explicitly request subsequent pages through `page` and use `TotalPages`. Notes: transactions can take up to three hours to appear and the call covers the previous three years; with optional filters, `ending_balance` is empty. | `operations/TransactionSearch.md`; `records-2-Pa-Ve.md` |
| `client.TransactionSearch.SearchBalances` | `SearchBalances(string? asOfTime, string? currencyCode, PayPalServerSdk.Core.RequestOptions? requestOptions = null, CancellationToken ct = default)` | Nullable `asOfTime` and `currencyCode` have no defaults and must be explicit. Wire query names: `as_of_time`, `currency_code`. | `PayPalServerSdk.Models.BalancesResponse`: `Balances (balances): IReadOnlyList<PayPalServerSdk.Models.BalanceInformation>?`, `AccountId (account_id): string?`, `AsOfTime (as_of_time): string?`, `LastRefreshTime (last_refresh_time): string?`. | `SdkException<SearchBalancesError>` Case A; `TryGetDefaultError(out PayPalServerSdk.Models.DefaultError)` [400,403,500], then `TryGetRawError`. | none. Notes: balances may take up to three hours to appear and covers up to the prior three years. | `operations/TransactionSearch.md`; `records-1-Ac-Pa.md` |

### Required enums and statuses

| Enum (fully qualified) | Literal member → wire value |
|---|---|
| `PayPalServerSdk.Models.Enums.CheckoutPaymentIntent` | `Capture` → `CAPTURE`; `Authorize` → `AUTHORIZE` |
| `PayPalServerSdk.Models.Enums.OrderStatus` | `Created` → `CREATED`; `Saved` → `SAVED`; `Approved` → `APPROVED`; `Voided` → `VOIDED`; `Completed` → `COMPLETED`; `PayerActionRequired` → `PAYER_ACTION_REQUIRED` |
| `PayPalServerSdk.Models.Enums.AuthorizationStatus` | `Created` → `CREATED`; `Captured` → `CAPTURED`; `Denied` → `DENIED`; `PartiallyCaptured` → `PARTIALLY_CAPTURED`; `Voided` → `VOIDED`; `Pending` → `PENDING` |
| `PayPalServerSdk.Models.Enums.CaptureStatus` | `Completed` → `COMPLETED`; `Declined` → `DECLINED`; `PartiallyRefunded` → `PARTIALLY_REFUNDED`; `Pending` → `PENDING`; `Refunded` → `REFUNDED`; `Failed` → `FAILED` |
| `PayPalServerSdk.Models.Enums.RefundStatus` | `Cancelled` → `CANCELLED`; `Failed` → `FAILED`; `Pending` → `PENDING`; `Completed` → `COMPLETED` |
| `PayPalServerSdk.Models.Enums.PaymentTokenStatus` | `Created` → `CREATED`; `PayerActionRequired` → `PAYER_ACTION_REQUIRED`; `Approved` → `APPROVED`; `Vaulted` → `VAULTED`; `Tokenized` → `TOKENIZED` |
| `PayPalServerSdk.Models.Enums.VaultStatus` | `Vaulted` → `VAULTED`; `Created` → `CREATED`; `Approved` → `APPROVED` |
| `PayPalServerSdk.Models.Enums.VaultTokenRequestType` | `SetupToken` → `SETUP_TOKEN` |
| `PayPalServerSdk.Models.Enums.PaymentInitiator` | `Customer` → `CUSTOMER`; `Merchant` → `MERCHANT` |
| `PayPalServerSdk.Models.Enums.StoredPaymentSourcePaymentType` | `OneTime` → `ONE_TIME`; `Recurring` → `RECURRING`; `Unscheduled` → `UNSCHEDULED` |
| `PayPalServerSdk.Models.Enums.StoredPaymentSourceUsageType` | `First` → `FIRST`; `Subsequent` → `SUBSEQUENT`; `Derived` → `DERIVED` |
| `PayPalServerSdk.Models.Enums.CardBrand` / `CardType` | `CardBrand`: `Visa` → `VISA`, `Mastercard` → `MASTERCARD`, `Discover` → `DISCOVER`, `Amex` → `AMEX`, `Unknown` → `UNKNOWN` (the map lists additional brands); `CardType`: `Credit` → `CREDIT`, `Debit` → `DEBIT`, `Prepaid` → `PREPAID`, `Store` → `STORE`, `Unknown` → `UNKNOWN` |

### Client, auth, server, idempotency, and configuration facts

| Concern | Contract |
|---|---|
| Package / namespace | Install `AsadAli.Checkout.Sdk` version `1.0.1`; root namespace is `PayPalServerSdk`; client is `PayPalServerSdk.PayPalServerSdkClient`; options are `PayPalServerSdk.PayPalServerSdkClientOptions`. |
| Construction | Constructor: `PayPalServerSdk.PayPalServerSdkClient(System.Net.Http.HttpClient httpClient, PayPalServerSdk.PayPalServerSdkClientOptions options)`. DI alternative: `AddPayPalServerSdkClient(Action<PayPalServerSdk.PayPalServerSdkClientOptions>)`. |
| Environment | `options.Environment` is `PayPalServerSdk.Servers.ServerEnvironment`; documented member is `PayPalServerSdk.Servers.ServerEnvironment.Sandbox`. Base URL override is through `options.Server`. |
| Auth | Options expose `Oauth2: PayPalServerSdk.Core.Authentication.OAuth2.ClientCredentials.OAuth2ClientCredentials?` and `Oauth2TokenStrategy: PayPalServerSdk.Core.Authentication.OAuth2.IOAuth2TokenStrategy<PayPalServerSdk.Core.Authentication.OAuth2.ClientCredentials.OAuth2ClientCredentials>?`; credentials fields are `ClientId (client_id): string` required, `ClientSecret (client_secret): string` required, and `Scope (scope): string?`. Set credentials before client construction. Source-level namespace confirmation: `PayPalServerSdkClientOptions.cs`, `Core/Authentication/OAuth2/ClientCredentials/OAuth2ClientCredentials.cs`, and `Core/Authentication/OAuth2/IOAuth2TokenStrategy.cs`. |
| Retry / timeout | `options.Retry` is `PayPalServerSdk.Core.Configuration.RetryOptions`; use `RetryOptions.Default()` or set all required members: `StatusCodesToRetry`, `HttpMethodsToRetry`, `MaxRetries`, `Delay`, `Timeout`, `BackOffFactor`, `UseExponentialBackoff`, `MaxJitter`, `OnRetry`. The companion skill governs safe values for non-idempotent writes. |
| Idempotency | SDK request-id parameters are literal `payPalRequestId` on `CreateOrder`, `AuthorizeOrder`, `CaptureOrder`, `CaptureAuthorizedPayment`, `RefundCapturedPayment`, `ReauthorizePayment`, and `CreatePaymentToken`/`CreateSetupToken`; pass the application’s stable replay key there. `VoidPayment` also exposes `payPalRequestId` but its generated order is after `payPalAuthAssertion`. No such parameter is present on `ListCustomerPaymentTokens`, reads, or searches. |
| Missing required flags | A request model may mark nothing required, and then `required?` selects nothing for you. Carry Notes-named fields: authorize/capture require buyer approval or a valid `payment_source`; refund full vs partial determines empty body vs `amount`; patching is not in this requested flow. |

## 3. Trap notes

- ⚠ Step 1 (client registration) — HTTP-client ownership, DI lifetime, and SDK-wrapper lifetime can be miswired. **MUST load `paypal-sdk:dotnet-client-initialization`**.
- ⚠ Step 1 (authentication) — credential property names, token strategy, environment binding, and secret loading are easy to mismatch. **MUST load `paypal-sdk:dotnet-authentication`**.
- ⚠ Step 1 (configuration) — retry triggers, transport failures, timeout scope, server-node selection, and logging affect write safety. **MUST load `paypal-sdk:dotnet-configuration-resilience`**.
- ⚠ Step 2 (endpoint calls) — nullable parameters without C# defaults must be passed explicitly and named arguments must preserve literal generated names, including `ct`. **MUST load `paypal-sdk:dotnet-calling-endpoints`**.
- ⚠ Step 2 (models) — JSON wire names, `required` members, StringEnum construction, and payment-source model composition can silently change the payload. **MUST load `paypal-sdk:dotnet-models`**.
- ⚠ Step 2 (error boundary) — typed errors, raw errors, no-content accessors, and exception translation have different access paths. **MUST load `paypal-sdk:dotnet-error-handling`**.
- ⚠ Step 5 (reconciliation tests) — fake the supported HTTP seam and assert mapped behavior, not SDK implementation details. **MUST load `paypal-sdk:dotnet-testing`**.

## 4. REQUIRED READING

Load these before implementation starts; this sheet deliberately does not carry their contents.

- `paypal-sdk:dotnet-client-initialization` · Step 1 client construction and DI.
- `paypal-sdk:dotnet-authentication` · Step 1 credentials and OAuth wiring.
- `paypal-sdk:dotnet-configuration-resilience` · Step 1 retry, timeout, server selection, logging, and Step 5 pagination.
- `paypal-sdk:dotnet-calling-endpoints` · Steps 2–5 operation invocation and cancellation.
- `paypal-sdk:dotnet-models` · Steps 2–4 request/response model construction and mapping.
- `paypal-sdk:dotnet-error-handling` · all operation boundaries and provider-error translation.
- `paypal-sdk:dotnet-testing` · reconciliation and integration-layer tests.

- a drifted or malformed **2xx** body (a missing `required` member) surfaces as a `JsonException` from deserialization, **not** as an `SdkException` — so an SDK-exception-only catch ladder lets it escape the integration boundary;
- a **non-2xx** body that does not match its operation's generated `{Operation}Error` shape throws `JsonException` *while the error object is being constructed*, so the `JsonException` **replaces** the `SdkException` and the HTTP status is destroyed with it — a boundary that maps every `JsonException` to a 5xx then reports a deterministic rejection as an outage, and a caller that retries 5xx retries something that can never succeed.

**MUST load `dotnet-error-handling`** before writing that boundary. These rows belong in the FIRST sheet, not a later revision: the boundary is written early, and a caveat that arrives afterwards arrives too late to shape it.

## 5. Assumptions & Blockers

- The requested “cancel” capability is authorized-payment cancellation, represented by `Payments.VoidPayment`; the map has no separate order-cancel operation.
- eShopOnWeb PublicApi endpoint routes, DTOs, persistence, caller identity, and replay/concurrency policy are application decisions and are not specified by the SDK map.
- `SearchTransactions` is the reconciliation source; the map documents up to three hours of reporting latency, previous-three-year coverage, and no SDK pagination helper. The implementer must choose the application’s polling window and cursor persistence.
