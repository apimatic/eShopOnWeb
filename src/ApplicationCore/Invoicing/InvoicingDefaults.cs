namespace Microsoft.eShopWeb.ApplicationCore.Invoicing;

public static class InvoicingDefaults
{
    /// <summary>The provider account bills in USD; every bill this application raises uses it.</summary>
    public const string Currency = "USD";
}
