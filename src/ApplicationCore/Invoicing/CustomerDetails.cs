namespace Microsoft.eShopWeb.ApplicationCore.Invoicing;

/// <summary>The customer details a bill carries. Invented per fixture in the sandbox.</summary>
public record CustomerDetails(string Name, string Email);
