# Maxio recurring-subscription integration plan

## Scope & sequence

| Step | Application work | Maxio operations | Outcome |
|---|---|---|---|
| 1 | Add NuGet dependency `AsadAli.AdvancedBilling.Sdk` at the map-pinned contract version `1.0.2`; bind exactly `Maxio:ApiKey`, `Maxio:Subdomain`, `Maxio:ProductFamilyHandle`, and optional `Maxio:BaseUrl`; validate required non-secret settings at startup; keep all values out of the repository and source the credential through the existing .NET configuration/user-secrets workflow. | Client construction only. | One long-lived HTTP pipeline and configured SDK client. (`sdk-map.md`) |
| 2 | Add an application-owned billing gateway/service so PublicApi endpoints do not depend directly on generated controllers. Flow cancellation tokens through every boundary. | All operations below through `MaxioAdvancedBilling.MaxioAdvancedBillingClient`. | Stable test seam and one error-translation boundary. (`sdk-map.md`) |
| 3 | Implement authenticated `GET /api/subscription-plans`. Page through the configured family using `productFamilyId: $"handle:{configuredHandle}"`, pass unused filters explicitly as `null`, set `includeArchived: false`, and map the family’s products to public plan DTOs. | `client.ProductFamilies.ListProductsForProductFamily`. | Plans keyed by stable product handles, never configured numeric IDs. (`operations/ProductFamilies.md`; handle syntax and page maximum confirmed in map-named `Api/ProductFamilies.cs`) |
| 4 | For authenticated `POST /api/subscriptions`, re-read the requested product by handle and verify its returned `ProductFamily.Handle` matches `Maxio:ProductFamilyHandle` before any write. | `client.Products.ReadProductByHandle`. | Server-side authorization of the requested plan against the configured Maxio family. (`operations/Products.md`, `records-3-Of-Su.md`) |
| 5 | Reconcile the Maxio customer from an immutable JWT user identifier: exact reference lookup; if absent, create with that reference plus required first name, last name, and email; if create returns 422, re-read by reference and accept only a now-existing customer. Never use email alone as identity. | `client.Customers.ReadCustomerByReference`, `client.Customers.CreateCustomer`. | Race-safe customer creation because Maxio explicitly requires customer `reference` uniqueness. (`operations/Customers.md`, `records-1-Ac-Cr.md`, `records-2-Cr-Ne.md`) |
| 6 | Before creating, derive a deterministic subscription reference and call the reference lookup. Do not claim this makes the write race-safe: strict subscription idempotency is blocked by the SDK/API contract described below. Once that blocker is resolved externally, create using product handle + customer reference + subscription reference and omit all payment-profile/card/bank fields. | `client.Subscriptions.FindSubscription`, then `client.Subscriptions.CreateSubscription`. | Return `plan`, `priceInCents`, `state`, and `nextBillingDate` from the Maxio response; no local billing state becomes authoritative. (`operations/Subscriptions.md`, `records-2-Cr-Ne.md`, `records-3-Of-Su.md`, `records-4-Su-We.md`) |
| 7 | Implement authenticated `GET /api/my-subscriptions`: resolve the caller’s customer by the same immutable reference, then use the returned runtime Maxio customer ID for the customer-scoped list. This runtime ID is not configuration and is never persisted as a seeded identifier. | `client.Customers.ReadCustomerByReference`, `client.Customers.ListCustomerSubscriptions`. | Current account subscriptions mapped from Maxio on every request. (`operations/Customers.md`, `records-3-Of-Su.md`, `records-4-Su-We.md`) |
| 8 | Add endpoint/service tests for JWT isolation, family filtering, manual pagination, exact envelopes, customer create races, typed/raw provider failures, cancellation, malformed success/error JSON, and the subscription-idempotency blocker/resolution. | HTTP seam of `MaxioAdvancedBillingClient(HttpClient, MaxioAdvancedBillingClientOptions)`. | Production-grade behavior without live Maxio traffic in tests. (`sdk-map.md`) |

## CONTRACT SHEET

> **Signatures are generated code, verbatim — every parameter name is the literal
> C# identifier. The cancellation-token parameter really is named `ct`: in named
> arguments write `ct:`, never `cancellationToken:`.**
>
> **Every SDK type is written fully-qualified with the namespace the map gives it** — take
> each one from that type's own map row, never from where a neighbouring type sits. A members
> table names the namespace outright; otherwise the row's source path implies it
> (`Core/Configuration/…` ⇒ `…Core.Configuration`; a file at the repo root ⇒ the root
> namespace). Enums, unions, auth, server and client-config types are spread across different
> child namespaces, and two types configured side by side in the same options object routinely
> live in different ones. Dropping a type to the root or to `.Models` makes the implementer
> guess the wrong `using`, and the build breaks.

### SDK identity, client, auth, and server

| Fact | Exact contract | Source |
|---|---|---|
| Package | NuGet `AsadAli.AdvancedBilling.Sdk`; map/source contract stamp `v1.0.2` / commit `15db14b`; root namespace `MaxioAdvancedBilling`; target `netstandard2.0`. | `sdk-map.md` |
| Client | `MaxioAdvancedBilling.MaxioAdvancedBillingClient`; only constructor: `MaxioAdvancedBillingClient(System.Net.Http.HttpClient httpClient, MaxioAdvancedBilling.MaxioAdvancedBillingClientOptions options)`. Controller properties used: `ProductFamilies`, `Products`, `Customers`, `Subscriptions`. | `sdk-map.md` |
| Options | `MaxioAdvancedBilling.MaxioAdvancedBillingClientOptions` properties: `Environment: MaxioAdvancedBilling.Servers.ServerEnvironment`, `Retry: MaxioAdvancedBilling.Core.Configuration.RetryOptions`, `Server: MaxioAdvancedBilling.ServerOptions`, `BasicAuth: MaxioAdvancedBilling.Core.Authentication.Basic.BasicAuthCredentials?`. | `sdk-map.md` |
| Basic auth | `options.BasicAuth = new MaxioAdvancedBilling.Core.Authentication.Basic.BasicAuthCredentials { Username = configuredApiKey, Password = "x" }`. Basic auth is the only SDK auth scheme. | `sdk-map.md` |
| Environment | `MaxioAdvancedBilling.Servers.ServerEnvironment.Us` has wire value `US` and Production template `https://{site}.chargify.com`; `.Eu` has value `EU` and template `https://{site}.ebilling.maxio.com`. The requested four-key configuration has no region key, so this plan carries the SDK default/US environment; a non-US site must supply `Maxio:BaseUrl`. | `sdk-map.md` |
| Derived URL | When `Maxio:BaseUrl` is absent, set `options.Server.Production.Us.Site` to `Maxio:Subdomain`; the SDK expands the Production US template. | `sdk-map.md` |
| Verbatim override | When `Maxio:BaseUrl` is present, assign that exact configured string to `options.Server.Production.Us.BaseUrl`; do not append the subdomain, path, slash, or any other derived text. These endpoints all use the Production server group, not Ebb. | `sdk-map.md` |
| Exact app configuration | Keys are exactly `Maxio:ApiKey` (required secret), `Maxio:Subdomain` (required by the stated app configuration), `Maxio:ProductFamilyHandle` (required), `Maxio:BaseUrl` (optional). No secret or sample value belongs in tracked files. | Task requirement; SDK destinations grounded in `sdk-map.md` |

### Operations

| Step / controller | Exact generated signature and HTTP | Request/filter contract | Return envelope and fields read | Error contract | Pagination | Source |
|---|---|---|---|---|---|---|
| Plans · `client.ProductFamilies` | `ListProductsForProductFamily(string productFamilyId, MaxioAdvancedBilling.Models.Enums.BasicDateField? dateField, MaxioAdvancedBilling.Models.ListProductsFilter? filter, System.DateTimeOffset? startDate, System.DateTimeOffset? endDate, System.DateTimeOffset? startDatetime, System.DateTimeOffset? endDatetime, bool? includeArchived, MaxioAdvancedBilling.Models.Enums.ListProductsInclude? include, int? page = 1, int? perPage = 20, System.Threading.CancellationToken ct = default)`; `GET /product_families/{product_family_id}/products.json`. First 8 nullable arguments have no C# defaults and MUST be passed. `productFamilyId` accepts an ID or a handle prefixed with `handle:`; therefore pass `$"handle:{Maxio:ProductFamilyHandle}"`. | Query wire names: `date_field`, `filter`, `start_date`, `end_date`, `start_datetime`, `end_datetime`, `include_archived`, `include`, `page`, `per_page`. `MaxioAdvancedBilling.Models.ListProductsFilter`: `Ids (ids): IReadOnlyList<int>?`, `PrepaidProductPricePoint (prepaid_product_price_point): MaxioAdvancedBilling.Models.PrepaidProductPricePointFilter?`, `UseSiteExchangeRate (use_site_exchange_rate): bool?`; this flow passes `filter: null`. | `System.Threading.Tasks.Task<System.Collections.Generic.IReadOnlyList<MaxioAdvancedBilling.Models.ProductResponse>>`. Each envelope has required `Product (product): MaxioAdvancedBilling.Models.Product`. Read `Name (name): string?`, `Handle (handle): string?`, `Description (description): string?`, `PriceInCents (price_in_cents): long?`, `Interval (interval): int?`, `IntervalUnit (interval_unit): MaxioAdvancedBilling.Models.Enums.IntervalUnit?`, `ArchivedAt (archived_at): System.DateTimeOffset?`, `RequestCreditCard (request_credit_card): bool?`, `RequireCreditCard (require_credit_card): bool?`, `ProductFamily (product_family): MaxioAdvancedBilling.Models.ProductFamily?`. | Case A: `MaxioAdvancedBilling.Core.Exceptions.SdkException<MaxioAdvancedBilling.Errors.ListProductsForProductFamilyError>`; `TryGetString(out string)` for 404; inherited `TryGetRawError(out MaxioAdvancedBilling.Core.ErrorResponse.RawError)` fallback. | Manual `page` + `perPage`; defaults 1/20. Generated source documents maximum `perPage` 200 and an empty result when no records remain. | `operations/ProductFamilies.md`; `records-3-Of-Su.md`; `Api/ProductFamilies.cs`; `enums.md` |
| POST plan validation · `client.Products` | `ReadProductByHandle(string apiHandle, System.Threading.CancellationToken ct = default)`; `GET /products/handle/{api_handle}.json`. | `apiHandle` is the stable product handle submitted by the application client. | `System.Threading.Tasks.Task<MaxioAdvancedBilling.Models.ProductResponse>`; required `Product (product): MaxioAdvancedBilling.Models.Product`. Read the same product fields above and nested `MaxioAdvancedBilling.Models.ProductFamily.Handle (handle): string?`. | Case B: `MaxioAdvancedBilling.Core.Exceptions.SdkException<MaxioAdvancedBilling.Core.ErrorResponse.RawError>`; inspect `StatusCode: System.Net.HttpStatusCode`, `ReadAsString()`, `ReadAsJson<T>()`, or `ReadAsBytes()`. | None. | `operations/Products.md`; `records-3-Of-Su.md` |
| Customer lookup · `client.Customers` | `ReadCustomerByReference(string reference, System.Threading.CancellationToken ct = default)`; `GET /customers/lookup.json`; query `reference`. | `reference` is required and is the application’s stable immutable JWT-derived customer reference. | `System.Threading.Tasks.Task<MaxioAdvancedBilling.Models.CustomerResponse>`; required `Customer (customer): MaxioAdvancedBilling.Models.Customer`. Read `Id (id): int?`, `Reference (reference): string?`, `FirstName (first_name): string?`, `LastName (last_name): string?`, `Email (email): string?`. | Case B: `MaxioAdvancedBilling.Core.Exceptions.SdkException<MaxioAdvancedBilling.Core.ErrorResponse.RawError>` with `StatusCode`, `ReadAsString()`, `ReadAsJson<T>()`, `ReadAsBytes()`. A missing customer is detected from the raw HTTP status, not a typed accessor. | None. | `operations/Customers.md`; `records-2-Cr-Ne.md` |
| Customer create · `client.Customers` | `CreateCustomer(MaxioAdvancedBilling.Models.CreateCustomerRequest? body, System.Threading.CancellationToken ct = default)`; `POST /customers.json`. Nullable `body` has no default and MUST be passed. | `MaxioAdvancedBilling.Models.CreateCustomerRequest.Customer (customer): MaxioAdvancedBilling.Models.CreateCustomer` is required. Nested required fields: `FirstName (first_name): string`, `LastName (last_name): string`, `Email (email): string`; `Reference (reference): string?` is optional in the C# model but MUST be populated for this reconciliation flow. The operation contract explicitly says only one customer may exist for a reference and reference must be unique when supplied. | `System.Threading.Tasks.Task<MaxioAdvancedBilling.Models.CustomerResponse>`; required `Customer (customer): MaxioAdvancedBilling.Models.Customer`; read `Id`, `Reference`, `FirstName`, `LastName`, `Email`. | Case A: `MaxioAdvancedBilling.Core.Exceptions.SdkException<MaxioAdvancedBilling.Errors.CreateCustomerError>`; `TryGetCustomerErrorResponse1(out MaxioAdvancedBilling.Models.CustomerErrorResponse1)` for 422; inherited `TryGetRawError(out MaxioAdvancedBilling.Core.ErrorResponse.RawError)` fallback. `MaxioAdvancedBilling.Models.CustomerErrorResponse1.Errors (errors): MaxioAdvancedBilling.Models.Errors?`; the mapped `Errors` fields (`PerPage`, `PricePoint`) are suspicious for customer validation, so do not infer duplicate-reference status from that shape: after 422, re-lookup; if absent, expose a generic validation failure. This payload trust limitation is map-visible. | None. | `operations/Customers.md`; `records-1-Ac-Cr.md`; `records-2-Cr-Ne.md` |
| Subscription lookup · `client.Subscriptions` | `FindSubscription(string? reference, System.Threading.CancellationToken ct = default)`; `GET /subscriptions/lookup.json`; nullable `reference` has no default and MUST be passed. | Query wire name `reference`. Generated source calls it the subscription reference; neither map nor source states it is unique. | `System.Threading.Tasks.Task<MaxioAdvancedBilling.Models.SubscriptionResponse>`; `Subscription (subscription): MaxioAdvancedBilling.Models.Subscription?` is nullable. Read mapped subscription fields below. | Case A: `MaxioAdvancedBilling.Core.Exceptions.SdkException<MaxioAdvancedBilling.Errors.FindSubscriptionError>`; `TryGetNoContent(out MaxioAdvancedBilling.Core.ErrorResponse.RawError)` for 404; inherited `TryGetRawError(out MaxioAdvancedBilling.Core.ErrorResponse.RawError)` fallback. | None. | `operations/Subscriptions.md`; `records-4-Su-We.md`; `Api/Subscriptions.cs` |
| Subscription create · `client.Subscriptions` | `CreateSubscription(MaxioAdvancedBilling.Models.CreateSubscriptionRequest? body, System.Threading.CancellationToken ct = default)`; `POST /subscriptions.json`. Nullable `body` has no default and MUST be passed. | `MaxioAdvancedBilling.Models.CreateSubscriptionRequest.Subscription (subscription): MaxioAdvancedBilling.Models.CreateSubscription` is required. Fields used: `ProductHandle (product_handle): string?`, `CustomerReference (customer_reference): string?`, `Reference (reference): string?`. Operation docs require one of product ID/handle and one of customer ID/reference/attributes; this flow uses the two handles/references and no numeric configuration. `PaymentProfileId`, `PaymentProfileAttributes`, `CreditCardAttributes`, and `BankAccountAttributes` are nullable and omitted. `PaymentCollectionMethod (payment_collection_method): MaxioAdvancedBilling.Models.Enums.CollectionMethod?` is optional and omitted; full enum values appear below. | `System.Threading.Tasks.Task<MaxioAdvancedBilling.Models.SubscriptionResponse>`; nullable `Subscription (subscription): MaxioAdvancedBilling.Models.Subscription?`. Read `Id (id): int?`, `Reference (reference): string?`, `State (state): MaxioAdvancedBilling.Models.Enums.SubscriptionState?`, `ProductPriceInCents (product_price_in_cents): long?`, `NextAssessmentAt (next_assessment_at): System.DateTimeOffset?`, and nested `Product (product): MaxioAdvancedBilling.Models.Product?` → `Name (name): string?`, `Handle (handle): string?`. These map directly to response `plan`, `priceInCents`, `state`, `nextBillingDate`. | Case A: `MaxioAdvancedBilling.Core.Exceptions.SdkException<MaxioAdvancedBilling.Errors.CreateSubscriptionError>`; `TryGetErrorListResponse1(out MaxioAdvancedBilling.Models.ErrorListResponse1)` for 422 (`Errors (errors): System.Collections.Generic.IReadOnlyList<string>` required); inherited `TryGetRawError(out MaxioAdvancedBilling.Core.ErrorResponse.RawError)` fallback. | None. | `operations/Subscriptions.md`; `records-2-Cr-Ne.md`; `records-3-Of-Su.md`; `records-4-Su-We.md`; `enums.md` |
| My subscriptions · `client.Customers` | `ListCustomerSubscriptions(int customerId, System.Threading.CancellationToken ct = default)`; `GET /customers/{customer_id}/subscriptions.json`. | `customerId` is the runtime `MaxioAdvancedBilling.Models.Customer.Id` returned by lookup. It is not a configured/seeded numeric identifier. | `System.Threading.Tasks.Task<System.Collections.Generic.IReadOnlyList<MaxioAdvancedBilling.Models.SubscriptionResponse>>`; each envelope’s `Subscription (subscription): MaxioAdvancedBilling.Models.Subscription?` is nullable. Read the subscription/product fields listed in the create row. | Case B: `MaxioAdvancedBilling.Core.Exceptions.SdkException<MaxioAdvancedBilling.Core.ErrorResponse.RawError>` with `StatusCode`, `ReadAsString()`, `ReadAsJson<T>()`, `ReadAsBytes()`. | None; this SDK operation exposes no page/filter arguments. | `operations/Customers.md`; `records-3-Of-Su.md`; `records-4-Su-We.md` |

### Enum value tables

All are string-enum wrappers in `MaxioAdvancedBilling.Models.Enums`, not C# enums. Use the literal static member or `FromValue(wireValue)`.

| Type / where used | Complete generated members and wire values | Source |
|---|---|---|
| `MaxioAdvancedBilling.Models.Enums.BasicDateField` · plans signature | `UpdatedAt (updated_at)`, `CreatedAt (created_at)` | `enums.md` |
| `MaxioAdvancedBilling.Models.Enums.ListProductsInclude` · plans signature | `PrepaidProductPricePoint (prepaid_product_price_point)` | `enums.md` |
| `MaxioAdvancedBilling.Models.Enums.CollectionMethod` · enum-typed field `MaxioAdvancedBilling.Models.CreateSubscription.PaymentCollectionMethod` | `Automatic (automatic)`, `Remittance (remittance)`, `Prepaid (prepaid)`, `Invoice (invoice)` | `enums.md` |
| `MaxioAdvancedBilling.Models.Enums.IntervalUnit` · plan response | `Day (day)`, `Month (month)` | `enums.md` |
| `MaxioAdvancedBilling.Models.Enums.SubscriptionState` · subscription response | `Pending (pending)`, `FailedToCreate (failed_to_create)`, `Trialing (trialing)`, `Assessing (assessing)`, `Active (active)`, `SoftFailure (soft_failure)`, `PastDue (past_due)`, `Suspended (suspended)`, `Canceled (canceled)`, `Expired (expired)`, `Paused (paused)`, `Unpaid (unpaid)`, `TrialEnded (trial_ended)`, `OnHold (on_hold)`, `AwaitingSignup (awaiting_signup)` | `enums.md` |

Enum audit: `MaxioAdvancedBilling.Models.CreateCustomerRequest` / `MaxioAdvancedBilling.Models.CreateCustomer` has no enum-typed field. `MaxioAdvancedBilling.Models.CreateSubscriptionRequest` wraps `MaxioAdvancedBilling.Models.CreateSubscription`; its only direct enum-typed field is `PaymentCollectionMethod: MaxioAdvancedBilling.Models.Enums.CollectionMethod?`, whose full values are listed above. No other request model is constructed by this scope. (`records-1-Ac-Cr.md`, `records-2-Cr-Ne.md`, `enums.md`)

### Idempotency capabilities and trust boundary

| Capability | Grounded fact / directive | Trust | Source |
|---|---|---|---|
| Customer de-duplication | `CreateCustomer` explicitly documents that only one customer may exist for a supplied `reference`; `ReadCustomerByReference` returns the exact match. Lookup → create with immutable reference → re-lookup after 422 is supported by the contract and closes concurrent customer-create races. | Verified by generated operation contract. | `operations/Customers.md` |
| Subscription reference | `CreateSubscription.Reference (reference)` is an optional app-provided reference and `FindSubscription(reference, ct)` returns one subscription response. No map row or generated source declaration says that reference is unique or that duplicate create is rejected. | `UNVERIFIED` for uniqueness; live/provider behavior cannot be assumed. | `operations/Subscriptions.md`; `records-2-Cr-Ne.md`; map-named `Models/CreateSubscription.cs` and `Api/Subscriptions.cs` |
| SDK idempotency token/header | `CreateSubscription` exposes only `body` and `ct`; `CreateSubscriptionRequest` exposes only the `subscription` envelope. There is no idempotency-key/header/token argument or request field in the generated surface. | Verified absent from generated surface. | `operations/Subscriptions.md`; `records-2-Cr-Ne.md` |
| Defensive recovery | **UNVERIFIED directive:** use a deterministic subscription reference, look it up before create, and after any ambiguous create outcome attempt a best-effort lookup and return it if found; otherwise return the generic mapped provider/availability error. This reduces duplicates but does not prove race-free exactly-once creation. | Defensive only; not a strict guarantee. | `operations/Subscriptions.md` |
| Strict requirement | The requested “retries/double-clicks never create duplicate subscriptions” guarantee cannot be implemented from the exposed Maxio SDK/API contract alone. Do not present preflight lookup as atomic. Implementation of the POST write is blocked until Maxio supplies a documented uniqueness/idempotency guarantee or the owner explicitly authorizes a durable application-side idempotency coordinator with a unique operation key. | **BLOCKER** | `operations/Subscriptions.md`; `records-2-Cr-Ne.md` |

### Error boundary facts

| Fact | Contract | Source |
|---|---|---|
| Throw-only | This SDK generates no no-throw result variants. Case A operations throw `MaxioAdvancedBilling.Core.Exceptions.SdkException<TypedError>`; Case B operations throw `SdkException<MaxioAdvancedBilling.Core.ErrorResponse.RawError>`. | `sdk-map.md` |
| Raw error | `RawError.StatusCode: System.Net.HttpStatusCode`; `ReadAsBytes(): ReadOnlyMemory<byte>`; `ReadAsString(): string`; `ReadAsJson<T>(): T?`. | `sdk-map.md` |
| Public boundary | Preserve authentication/authorization failures locally; translate Maxio not-found/validation/conflict/provider/transport conditions without returning API keys, raw provider bodies, or exception strings. Exact catch ordering and mappings are governed by required reading, not invented here. | Operation rows above; `sdk-map.md` |

### Test seam and contract fixtures

| Test concern | SDK-grounded seam/assertion | Source |
|---|---|---|
| Unit/contract seam | Construct `MaxioAdvancedBillingClient` with an `HttpClient` backed by a fake `HttpMessageHandler`; no generated controller interface is exposed by the contract sheet. Assert method/path/query/body at the HTTP boundary. | `sdk-map.md` |
| Server selection | Cover absent `BaseUrl` → configured `Production.Us.Site`; present `BaseUrl` → exact `Production.Us.BaseUrl`, with no derivation. | `sdk-map.md` |
| Catalog fixture | Response is a JSON array of envelopes, each shaped `{ "product": { ... } }`; assert `handle:{familyHandle}`, explicit optional query behavior, `include_archived=false`, and pages through an empty/short final page. | `operations/ProductFamilies.md`; `records-3-Of-Su.md` |
| Customer fixture | Lookup/create responses are `{ "customer": { ... } }`; exercise lookup hit, 404 then create, and 404 → concurrent create 422 → lookup hit. | `operations/Customers.md`; `records-1-Ac-Cr.md`; `records-2-Cr-Ne.md` |
| Subscription fixture | Create/find response is `{ "subscription": { ... } }`; customer list is an array of those envelopes. Exercise nullable `subscription`, state values, nested product, price, and next assessment date. | `operations/Subscriptions.md`; `operations/Customers.md`; `records-3-Of-Su.md`; `records-4-Su-We.md` |
| Error fixtures | Cover each exact Case A accessor above, Case B raw statuses, malformed 2xx JSON, malformed typed non-2xx JSON, cancellation, and ambiguous create transport outcomes. Do not assert exception `.ToString()`. | `sdk-map.md`; operation rows above |
| Auth isolation | Integration tests must prove no customer reference can be supplied by the request body and that each endpoint derives identity only from the authenticated JWT/user context. | Task requirement; customer reference contract in `operations/Customers.md` |

## Trap notes

⚠ Step 1 (client registration) — HTTP pipeline ownership and generated-client lifetime determine socket use and whether tests can replace transport. **MUST load `dotnet-client-initialization`** before registering the client.

⚠ Step 1 (credentials) — the Basic credentials object and configuration timing determine whether every call is authenticated without leaking the key. **MUST load `dotnet-authentication`** before wiring credentials.

⚠ Steps 3–7 (calls) — nullable arguments without C# defaults must still be supplied, and response payloads sit inside generated envelopes. **MUST load `dotnet-calling-endpoints`** before the first call.

⚠ Steps 3–7 (models) — required initializers, nullable envelope members, string-enum wrappers, and wire names can otherwise produce compile failures or silent mapping errors. **MUST load `dotnet-models`** before constructing or mapping models.

⚠ Steps 3–7 (resilience/idempotency) — retry/timeout behavior and ambiguous POST transport failures affect whether a subscription write can be re-sent and duplicated; pagination is also manual. **MUST load `dotnet-configuration-resilience`** before configuring resilience or the plan loop.

⚠ Steps 5–7 (errors) — Case A and Case B exceptions have different payload access paths, and raw exception text is not a safe API response. **MUST load `dotnet-error-handling`** before writing the integration boundary.

- a drifted or malformed **2xx** body (a missing `required` member) surfaces as a `JsonException` from deserialization, **not** as an `SdkException` — so an SDK-exception-only catch ladder lets it escape the integration boundary;
- a **non-2xx** body that does not match its operation's generated `{Operation}Error` shape throws `JsonException` *while the error object is being constructed*, so the `JsonException` **replaces** the `SdkException` and the HTTP status is destroyed with it — a boundary that maps every `JsonException` to a 5xx then reports a deterministic rejection as an outage, and a caller that retries 5xx retries something that can never succeed.

**MUST load `dotnet-error-handling`** before writing that boundary.

⚠ Step 8 (tests) — the transport seam, error fixtures, and assertions must cover behavior without binding tests to generated controller internals. **MUST load `dotnet-testing`** before writing tests.

## REQUIRED READING

Load all of the following **before implementation starts**. This sheet deliberately does not carry their contents.

- `dotnet-client-initialization` · Step 1 client construction, `HttpClient` ownership, and DI.
- `dotnet-authentication` · Step 1 Basic credentials and secret/configuration wiring.
- `dotnet-calling-endpoints` · Steps 3–7 exact call style, optional arguments, envelopes, async, and cancellation.
- `dotnet-models` · Steps 3–7 required records, nullable members, wire names, and string enums.
- `dotnet-configuration-resilience` · Steps 1, 3, and 6 server override, retries/timeouts, write ambiguity, and pagination.
- `dotnet-error-handling` · Steps 3–7 typed/raw errors, `JsonException`, and the public exception boundary.
- `dotnet-testing` · Step 8 `HttpClient` seam and behavioral coverage.

## Assumptions & Blockers

- **Assumption:** The authenticated identity exposes an immutable stable user identifier plus non-empty first name, last name, and email (directly as claims or through the app’s existing user profile). `CreateCustomer` requires all three name/email values; if they are unavailable, customer creation is blocked rather than fabricated.
- **Assumption:** The configured products are intentionally set up to allow subscription creation without a payment method. The operation contract says payment information may be required depending on product options, and product responses expose `RequestCreditCard` / `RequireCreditCard`; a product that requires payment details is outside this no-payment-method flow and blocks creation.
- **Assumption:** With no region key in the mandated configuration shape, normal subdomain-derived traffic uses the SDK’s US Production environment. An EU deployment must provide `Maxio:BaseUrl` under the stated configuration contract.
- **BLOCKER:** The SDK/API surface documents uniqueness for customer references but not for subscription references and exposes no idempotency key/header. Therefore it cannot substantiate the strict requirement that concurrent double-clicks, retries, or ambiguous POST outcomes never create duplicate subscriptions. The POST write must not be represented as production-grade exactly-once until Maxio documents such a guarantee or the application owner explicitly authorizes a durable application-side idempotency coordinator.
