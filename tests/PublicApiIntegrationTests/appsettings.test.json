{
  "UseOnlyInMemoryDatabase": true,
  "_comment": "Placeholder Maxio values so the fail-fast host boots under test. These are NOT real credentials; the integration tests never call the billing provider.",
  "Maxio": {
    "ApiKey": "test-api-key-not-real",
    "Subdomain": "test-subdomain",
    "ProductFamilyHandle": "test-family",
    "BaseUrl": ""
  }
}
