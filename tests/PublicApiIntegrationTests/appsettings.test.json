{
  "UseOnlyInMemoryDatabase": true,
  "_PayPal_comment": "Obviously-fake placeholders, not secrets. Program.cs loads this file after user-secrets, so the integration-test host cannot pick up a developer's real PayPal credentials and reach the live sandbox. They exist only so the host's startup validation passes; these tests exercise no payment endpoint.",
  "PayPal": {
    "ClientId": "test-client-id",
    "ClientSecret": "test-client-secret",
    "Environment": "sandbox",
    "Currency": "USD",
    "BaseUrl": "https://paypal.invalid"
  }
}
